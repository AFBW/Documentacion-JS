// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.1);                     // Ceil siempre redondea hacia arriba
resultado = Math.floor(2.9);                    // Floor siempre redondea hacia abajo
resultado = Math.sqrt(144);                     // Raiz Cuadrada
resultado = Math.abs(-200);                     // Convierte en +
resultado = Math.min(3, 5, 1, 8, 2, 10);        // El menor de todos
resultado = Math.max(3, 5, 1, 8, 2, 10);        // El mayor de todos
resultado = Math.random();                      // Aleatorio
resultado = Math.floor(Math.random() * 30);     // Aleatorios, Enteros entre 0-30

console.log(resultado);